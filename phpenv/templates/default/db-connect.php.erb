<?php
    /*
     * Environment Variables for application <%= @application %>
     * This will not work because we need a format like this define('S3_BUCKET', '<%= @s3bucket%>');
     */


    define('DB_HOST',     '<%= @host%>');
    define('DB_USER',     '<%= @user%>');
    define('DB_PASSWORD', '<%= @password%>');
    define('DB_NAME',     '<%= @db%>');